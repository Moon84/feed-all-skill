# Design

The future fetch layer should use stable identification, per-host request
limits, conditional requests, Retry-After handling, redirect validation, and
clear stop conditions for authorization and anti-bot responses.
