# Design

The future implementation should separate ephemeral article retrieval from
persistent entry metadata, define bounded retention settings, and expose
storage statistics before cleanup. Until released, these are planned design
items only.
