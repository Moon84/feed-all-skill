# Data Retention

## Current Contract

The released 0.2.x service persists source and entry metadata. A requested
article read may also persist fetched content.

## Target Direction

The target policy is link-first: bounded metadata is persistent and full text is
ephemeral unless a user-confirmed upstream workflow stores it.
