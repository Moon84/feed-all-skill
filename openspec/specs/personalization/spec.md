# Personalization

## Design Contract

An Agent may read a user-owned profile to rank entries and form discovery
queries. Profile changes require user confirmation. The feed service remains a
source and entry service rather than the owner of the user's long-term profile.
