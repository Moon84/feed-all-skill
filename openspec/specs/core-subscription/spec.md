# Core Subscription

## Current Contract

The released skill exposes RSS, Atom, and webpage subscription workflows
through MCP tools. Subscriptions persist source metadata locally and can be
refreshed by a host Agent.

The service does not own scheduling or digest generation. Host Agents may
invoke refresh from user-configured scheduled tasks.
